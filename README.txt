Решение содержит файлы:
    main.c - основная программа
    plugin_api.h 
    Makefile
    valgrind.txt

    libaerN3253.c - динамическая библиотека, реализующая задание варианта 3:

    Опция: --ipv4-addr <значение>
    Назначение: поиск файлов, содержащих заданное значение IPv4-адреса либо в
    текстовой форме (x.x.x.x), либо в бинарной (little-endian или big-endian).
    Пример: --ipv4-addr 192.168.8.1

    libsaaN3251.c - динамическая библиотека, реализующая задание варианта 1:

    Опция: --crc16 <значение>
    Назначение: поиск файлов, контрольная сумма которых, рассчитанная с помощью
    алгоритма CRC-16-CCITT, равна заданному значению. Значение суммы задается
    строкой, содержащей запись числа либо в двоичной (0b...), либо в десятичной,
    либо шестнадцатеричной (0x...) системах.
    Пример: --crc16 0xbabe


Пример запуска программы:

   ./lab1saaN3251 --ipv4-addr 123.34.5.6  {some_dir}
   ./lab1saaN3251 --crc16 10765  {some_dir}
   ./lab1saaN3251 -P {some_dir} --ipv4-addr 10673  {some_dir}
   ./lab1saaN3251 -v
   ./lab1saaN3251 -h

> > > Основная программа

Идентификация библиотек осуществялется с помощью проверки имени файла на наличие подстроки ".so"

> > > Плагин:  

Валидация входного значения iPv4 осуществялется с помощью функции inet_addr()